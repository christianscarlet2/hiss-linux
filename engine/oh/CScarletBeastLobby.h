#ifndef INC_CSCARLETBEASTLOBBY_H
#define INC_CSCARLETBEASTLOBBY_H

// Opens the embedded Scarlet Beast lobby in a WebView2 window so the operator
// can pick tables/tournaments for the bot from inside Hiss. Falls back to the
// default browser if the WebView2 environment cannot be created.
void SB_ShowLobby();

#endif  // INC_CSCARLETBEASTLOBBY_H
